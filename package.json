{
  "name": "portfolio",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "clean": "node -e \"const fs=require('node:fs'); fs.rmSync('.next', { recursive: true, force: true }); fs.rmSync('.next-dev', { recursive: true, force: true });\"",
    "clean:dev": "node -e \"const fs=require('node:fs'); fs.rmSync('.next-dev', { recursive: true, force: true });\"",
    "clean:prod": "node -e \"const fs=require('node:fs'); fs.rmSync('.next', { recursive: true, force: true });\"",
    "dev": "node scripts/run-next-dev.cjs",
    "dev:clean": "pnpm run clean:dev && node scripts/run-next-dev.cjs",
    "dev:reset": "node scripts/kill-stale-next-dev.cjs && pnpm run clean:dev && node scripts/run-next-dev.cjs",
    "build": "pnpm run clean:prod && next build",
    "start": "next start"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.1.17",
    "autoprefixer": "^10.4.22",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.17"
  },
  "dependencies": {
    "next": "^15.0.0",
    "react": "^19.2.3",
    "react-dom": "^19.2.3"
  },
  "packageManager": "pnpm@10.2.0",
  "engines": {
    "node": ">=20 <23"
  }
}
